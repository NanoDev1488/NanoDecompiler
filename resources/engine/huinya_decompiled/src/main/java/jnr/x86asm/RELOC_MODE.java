// исходный (обфусцированный) внутренний класс: jnr.x86asm.RELOC_MODE
package jnr.x86asm;

public enum RELOC_MODE {

    RELOC_NONE,
    RELOC_OVERWRITE;

  private RELOC_MODE() { // было: <init>
        // (пустое тело)
    }

}
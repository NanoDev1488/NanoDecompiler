// version.hpp - единственное место с версией движка (HANDOFF_46) - раньше
// строка NANO_DECOMPILER_VERSION дублировалась бы в cli_main.cpp/api.cpp
// по отдельности, теперь один источник для обоих.
#pragma once

namespace nd {
// НОВОЕ v1.7.3: проект выходит из статуса BETA - у релиза теперь
// кодовое имя вместо суффикса "BETA". "Ясность" выбрано осознанно: этот
// релиз продолжает серию фиксов, которые заменяют молчаливые
// упрощения/потери информации (см. CHANGELOG_v1.7.3.md - реконструкция
// enum-констант, честные футеры пустых классов и т.п.) на прозрачный,
// объяснимый результат вместо "чёрного ящика".
//
// БАГ-ФИКС (найдено при добавлении этой же строки): `--version` вручную
// собирает JSON конкатенацией (`cli_main.cpp`, без экранирования) -
// обычные ASCII-кавычки " внутри этой строки сломали бы JSON для
// electron/updater.ts. Кавычки-ёлочки «» - другие Unicode-символы,
// никак не пересекаются с JSON-синтаксисом, экранировать не нужно.
// НОВОЕ v1.7.4 «Устойчивость»: релиз сфокусирован на крашах Windows -
// нелегальные байтовые последовательности при конвертации путей
// (fs::u8path вместо fs::path), отсутствующие DLL при статической
// линковке (zlib/winpthread), необработанные исключения на верхнем
// уровне main(). Название отражает суть релиза лучше, чем техническое
// "Windows Bug Fix" - именно устойчивость к крашам, а не единственно
// платформа.
// НОВОЕ v1.7.8 «Точность»: сессия была про ложные срабатывания в детекции
// "что своё, а что чужое" - реальный jar пользователя (paper-*-nanocore.jar,
// Paperclip-загрузчик) вскрыл, что префикс "io.papermc" в known_libs()
// (pom_builder.cpp) matчил вообще ЛЮБОЙ io.papermc.* подпакет, включая
// io.papermc.paperclip - собственный код загрузчика, который должен был
// декомпилироваться, а не считаться paper-api и пропускаться целиком (все
// 259 классов таким образом уходили в library_classes_skipped, 0 из 0
// методов - выглядело как "не декомпилируется вообще", см. HANDOFF_URGENT
// п.15). Сузил префикс до "io.papermc.paper" - теперь matчит только реальный
// paper-api пакет. Вторая часть той же жалобы ("META-INF всё ещё остаётся") -
// Paperclip бандлит ПОЛНЫЙ Maven-кэш зависимостей сервера под
// META-INF/libraries/**/*.jar(.patch) + бинарные bsdiff-патчи под
// META-INF/versions/**/*.jar.patch - десятки-сотни МБ, которые раньше
// копировались как обычные "ресурсы" (process_jar.cpp) - теперь
// отфильтровываются тем же способом, что и META-INF/LICENSE*/proguard/* -
// вывод для этого jar'а упал с 52 МБ до 204 КБ, диагностика подтверждена
// diff -rq по всему регрессионному корпусу (0 отличий на остальных 17 jar).
// Плюс п.16: детальные строки малварь-находок ("  [!!!] описание (где)")
// под заголовком не содержали слова "вредоносн" и красились как обычный
// info вместо warning (engine.tsx, classifyLine()) - теперь ловятся по
// маркеру-тегу находки отдельно. И п.2: список пропущенных библиотечных
// классов в PluginDetailsModal.tsx больше не голый join(", ") на всю
// ширину модалки при 10+ библиотеках - первые 3 + разворачиваемый счётчик
// остальных.
// НОВОЕ v1.9.6 «Наведение порядка»: большая сессия правок GUI без
// изменений в самом движке декомпиляции - переключатель языка (был
// мёртвый код без элемента управления в разметке), сворачивание
// цветовых MC-цепочек и логгер-вызовов Bukkit в чип "как VS Code",
// группировка кнопок тулбара, честные сообщения об ошибках вьюера
// (бинарники/слишком большие файлы больше не показывают бесполезное
// "Повторить"), убрано редактируемое поле IP телеметрии и лишняя ручная
// кнопка отчёта, компактное окно первого запуска, popt/apt-попытка
// установки Java/Maven на Linux ПЕРЕД portable-скачиванием (см.
// install_jdk/install_maven ниже) - конкретно нацелена на повторяющуюся
// жалобу "Maven не ставится" (mirror-fallback самого скачивания уже
// чинился в v1.8.0, но проблема осталась - системный apt-путь на Linux
// обходит сетевые зеркала Apache целиком, а не просто добавляет ещё
// одно зеркало). Движок декомпиляции (байткод и т.п.) в этом релизе НЕ
// трогался - это последний чисто GUI-релиз перед серией 1.10.x,
// нацеленной на сам движок.
// НОВОЕ v1.9.7 «Källa» (источник): легитимность плагинов - "RuSpigot"
// (spigotmc.ru) убран из источников проверки по умолчанию (найдено прямое
// предупреждение сообщества - искать плагины строго на spigotmc.org, не
// на ".ru"-копиях, известный вектор бэкдоров; живого подтверждения, что
// spigotmc.ru вообще ещё официален, тоже не нашлось - инструмент проверки
// легитимности не должен сам держать сомнительный источник в доверенных).
// Вместо него - Hangar (hangar.papermc.io, официальный репозиторий
// плагинов PaperMC), новый SiteKind::HangarApi в legitimacy_check.cpp.
// GitHub/Modrinth/SpigotMC(Spiget) - все три перепроверены веб-поиском,
// подтверждены актуальными. См. legitimacy_sites.json и
// resources/engine/LEGITIMACY_SITES_MINI_LANGUAGE_SPEC.md.
// НОВОЕ v1.9.8: окно логов разработчика (Ctrl+Alt+L, затем O, затем G -
// буквальный chord "Ctrl+L+O+G" конфликтовал с уже занятыми Ctrl+L/Ctrl+O,
// см. engine.tsx) - второе BrowserWindow, источник записей - toast().
// НОВОЕ v1.9.9: read-write вьюер кода (HANDOFF п.6) - кнопка "Редактировать"
// в CodeView.tsx, textarea-режим (БЕЗ живой подсветки при печати - для неё
// нужен CodeMirror/Monaco, отдельная зависимость), fs:writeTextFile в
// main.ts (симметрично fs:readTextFile, тот же resolveWithinRoot).
// НОВОЕ v1.9.10: см. HANDOFF_NEXT_AGENT_HANDOVER.md, раздел "Волна 5" -
// z-index дропдаунов (уходили под Toasts), Ctrl+Alt+L для окна логов
// заменён на F12 (Ctrl+Alt+L оказался системным шорткатом блокировки
// экрана в GNOME - серьёзный баг предыдущей версии), баг сворачивания
// логгер-вызовов в javaHighlight.tsx (метод после строки исчезал вместо
// показа), read-write вьюер кода переделан на оверлей textarea поверх
// подсветки (Ctrl+S + автосохранение 5с, кнопка входа в режим убрана).
// ОСТАНОВЛЕНО ПО ПРОСЬБЕ ПОЛЬЗОВАТЕЛЯ с большим списком нерешённого -
// см. хэндофф, это НЕ финальная версия, а контрольная точка.
constexpr const char* NANO_DECOMPILER_VERSION = "NanoDecompiler v1.9.12";
}

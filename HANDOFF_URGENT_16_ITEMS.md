# ПРОЧИТАЙ ЭТОТ ФАЙЛ ПЕРВЫМ - 16 пунктов от пользователя, не сделано

Пользователь дал 16 пунктов в конце сессии v1.7.7 - НЕ УСПЕЛ сделать
из-за лимита контекста. Разбирай по одному, тестируй на реальных jar
(testjars_all/), полная регрессия перед каждой пачкой правок.

1. **"Время декомпиляции - ВРЁТ"** - PluginDetailsModal.tsx показывает
   `fmtSeconds(job.elapsedMs/1000)`, но elapsedMs может считаться
   неверно (например включает время ожидания в очереди, а не только
   сам прогон). Проверь `finalize()`/`startedAtRef` в engine.tsx.

2. **Список пропущенных библиотечных классов некрасивый** - в
   PluginDetailsModal.tsx `library_names_hit.join(", ")` - при много
   библиотек (10+) превращается в длинную нечитаемую строку. Сделать
   компактный список/сворачивание.

3. **Детектор "Неизвестно"** - сейчас `platform` в stats_json.cpp/
   process_jar.cpp детектит только Bukkit/Spigot/Sponge/Velocity/
   BungeeCord через plugin.yml/bungee.yml/velocity-plugin.json. Нужно
   добавить: детект клиентских модов (Fabric/Forge - fabric.mod.json/
   mods.toml), самописных утилит без платформенных маркеров - и
   честное "неизвестно" вместо пустого/неверного значения.

3-бис. **Почему баннер только в начале, а не логи построчно** - engine
   в обычном режиме и так пишет построчно (stage-маркеры, progress-бар,
   методы) - возможно, пользователь имел в виду GUI-терминал буферизует
   вывод пачками (см. `flushLines` в main.ts) - проверь задержку между
   реальным появлением строки в stdout и её показом в Terminal.tsx.

5. **pom.xml генерируется даже для НЕ-плагинов** - `build_pom()` в
   process_jar.cpp должен пропускаться, если platform полностью
   неизвестна (см. пункт 3) - сейчас, похоже, генерирует всегда.

6. **Декомпиляция вложенных jar** - jar внутри jar (например shaded
   зависимости или bundled-инструменты) + сканирование .py/.sh/.bat
   внутри на малварь + диалог "декомпилировать найденный(е) .jar
   тоже?" - БОЛЬШАЯ новая фича, требует: (а) детект embedded .jar при
   сканировании zip entries, (б) UI confirm-диалог, (в) рекурсивный
   вызов process_jar.

7. **~10 пунктов из СТАРЫХ списков не сделаны** - см. предыдущие
   HANDOFF в этом же файле/архиве и историю conversation (не
   сохранена локально - спроси пользователя, если он может её
   прислать, или ищи по чату). Известные незакрытые: настоящее дерево
   для Java-пакетов (не только ресурсов), hex-viewer бинарников,
   VS-Code-style персистентность collapse-state дерева файлов
   (сделана только для sortMode), редактируемый код во вьюере,
   компактная группировка кнопок тулбара (слишком много кнопок),
   аналитика/телеметрия (ждала вашего решения о scope - см. обсуждение
   про "без .jar, только текст ошибки").

8. **FancyFont.style()-подобные методы** - в NanoForge есть кастомный
   `method1("Text.")`-паттерн (вероятно транслитерация/спецсимволы для
   красивого текста) - нужно детектить ТАКИЕ методы (по имени/сигнатуре
   "принимает String, используется в цепочке с ChatColor") и применять
   их ТОЖЕ как псевдо-цвет/форматирование в подсветке, ОДНОЙ строкой
   вместе с ChatColor.RED + "..." + method1(...) - расширение
   mcColors.ts/javaHighlight.tsx. ВАЖНО: если одна и та же цветовая
   функция встречается ДВАЖДЫ в одной строке с разными цветами - не
   перепутать состояние (не глобальная переменная цвета на файл, а
   строго локальная на строку, см. текущий renderLine()).

9. **Скрытие/сворачивание цветового форматирования "как в VS Code"** -
   идея: `ChatColor.RED + "текст"` визуально схлопывается в ОДИН
   компактный цветной/жирный блок (как VS Code сворачивает inline
   decorators), разворачивается по двойному клику назад в исходный код.
   Технически сложно - требует интерактивности поверх статичного
   рендера токенов (сейчас renderLine() чисто функциональный). Нужно
   состояние "развёрнуто/свёрнуто" per-token, скорее всего per-line.

10. **"if/else лес" в NanoCommand (NanoForge)** - вероятно, это ТА ЖЕ
    хэшкод-switch(String) проблема (пункт из этой сессии,
    stackvm.cpp::collapse_string_switches), НО если структуризация НЕ
    смогла построить чистый switch вообще (например args.length
    проверки вложены иначе) - возможно, это отдельный, НЕ покрытый
    текущим паттерном случай if-else-цепочки. Проверь конкретно
    NanoCommand.java после текущего фикса - если всё ещё "лес" - это
    НЕ switch(String) паттерн, а что-то другое (возможно, реальный
    авторский if-else, который так и был написан - тогда это не баг).

11. **Подсказки при наведении для ВСЕХ цветовых детектов** - сейчас
    `title=` есть у mc-code-marker/mc-code-swatch/named-color-text (см.
    renderNamedColorText/renderUnknownColorMarked в mcColors.ts) - но
    ПРОВЕРЬ, что renderMcColored() (сырые &/§ коды) ТОЖЕ везде имеет
    title на КАЖДОМ цветном span, не только на marker.

12. **`$` в именах методов/классов не убираются** - обфусцированные/
    синтетические имена с `$` (например `Foo$Bar`, `lambda$method$0`)
    должны переименовываться renamer'ом. Проверь renamer.cpp - похоже,
    паттерн `$` не полностью покрыт эвристикой переименования.

13. **`__stk1`, `__stk2`, `__stk3` слишком много** - служебные имена
    временных стек-переменных decompiler'а, которые НЕ удалось
    заинлайнить обратно в выражения (см. `crossing_temp_types` в
    stackvm.cpp/engine.cpp). Много таких имён = слабое инлайнинг
    выражений. Стоит проверить, можно ли агрессивнее инлайнить простые
    одноразовые temp'ы прямо в место использования.

14. **`errorOut[0] = "..."` в выводе** - похоже на паттерн
    single-element-array-как-mutable-reference (javac trick для
    захвата переменной в лямбде/анонимном классе) - decompiler должен
    распознавать `final Object[] errorOut = new Object[1];` +
    `errorOut[0] = ...` внутри лямбды и превращать это обратно в
    обычную захваченную переменную, а не оставлять индексацию массива.

15. **Paper-1.*.*-nanocore не декомпилируется вообще (0 java файлов)
    + META-INF всё ещё остаётся** - ПОЛЬЗОВАТЕЛЬ ПРИШЛЁТ ЭТОТ JAR
    ОТДЕЛЬНО - как только придёт, воспроизведи и разберись. Скорее
    всего это НЕ обычный Bukkit-плагин (может быть Paper API jar,
    может содержать modules/multi-release структуру - см. незакрытый
    вопрос про META-INF/versions/ из более ранней сессии) - НЕ гадай,
    дождись файла и посмотри реальную структуру через `unzip -l`.

16. **Строки про малварь в консоли должны быть warning, не error/info**
    - проверь classifyLine() в engine.tsx - должно быть уже покрыто
    ("вредоносн" паттерн -> "err"), но пользователь мог иметь в виду
    ДРУГИЕ малварь-related строки (например legitimacy_check.cpp
    вывод) - перепроверь ВСЕ малварь/легитимность-related строки,
    которые движок печатает, и их классификацию в GUI.

## Общие правила (уже установлены пользователем в этой сессии)

- Каждые 5-10 исправленных пунктов - собери архив и отдай.
- НЕ гоняй полную регрессию на КАЖДУЮ мелкую правку - только после
  накопления нескольких правок или перед архивом. Компиляция C++ -
  единственная быстрая проверка, регрессия (33 jar) - только когда
  реально нужно (правил структуру/декомпиляцию).
- Skриншоты через Playwright-мокапы (не настоящий GUI - его нельзя
  собрать в песочнице без npm) для каждой значимой GUI-фичи - кидать
  в чат.
- global.d.ts и preload.ts ДОЛЖНЫ быть синхронны - при добавлении
  любого нового window.nano.* метода, объявляй его СРАЗУ в обоих
  местах (в этой сессии минимум 2 раза забывал именно про global.d.ts).
